package org.ideascale.repository;

import org.ideascale.model.CustomFiledValue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CustomFiledValueRepository extends JpaRepository<CustomFiledValue, Long> {
}